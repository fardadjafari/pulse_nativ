import 'package:flutter/material.dart';

class AboutUsPage  extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: new Text("Hello AboutUs Page",style: TextStyle(fontWeight: FontWeight.w600,fontSize: 27),),
    );
  }
}
